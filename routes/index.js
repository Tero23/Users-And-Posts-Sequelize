const userRouter = require('./user');
const postRouter = require('./post');
const likeRouter = require('./like');
const commentRouter = require('./comment');

module.exports = { userRouter, postRouter, likeRouter, commentRouter };
